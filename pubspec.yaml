name: credo_engage
description: A new Flutter project.

publish_to: "none"

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  image_picker: ^0.8.5+3
  lottie: ^1.3.0
  provider: ^6.0.2
  share_plus: ^4.0.6
  websafe_svg: ^2.1.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/svg/
    - assets/animations/
